/**
 * Implementation of Oppens "Efficient but Less Simple Algorithm"
 */

import { Token } from "./oppen-tokens"

class Scanner {
  scan() {
    let x
    while ((x = this.receive())) {

    }
  }

  receive(): Token {
    return null
  }
}